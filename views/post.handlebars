<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
</head>
<body>
  {{> post post}}
  
  <h1 style="margin-left:2em;">Comments</h1>
  {{#each post.comments}}
    {{> comment this}}
  {{else}}
    <p style="margin-left: 5em;">No comments yet. Be the first to comment!</p>
  {{/each}}
  
  <section class="new-comment-section">
    {{#if logged_in}}
      <form id="new-comment-form">
        <div class="form-group">
          <textarea id="comment-content" name="content" rows="3" required></textarea>
        </div>
        <div class="form-group">
          <button type="submit">Leave Comment</button>
        </div>
      </form>
    {{else}}
    <div class="form-group">
      <p><a href="/login">Log in</a> to leave a comment.</p>
    </div>      
    {{/if}}
    <div class="form-group">
      <a href="/">Back to Home</a>
    </div>
  </section>
  <script src="/js/script.js"></script>
</body>
</html>
